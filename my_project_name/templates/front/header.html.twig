<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link text-primary" href="{{path('annonces')}} "><i class="fas fa-megaphone"></i> Annonces</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-primary" href="{{path('liste-profils')}}"><i class="fas fa-people"></i> Les profiles</a>
                </li>
                 <li class="nav-item">
                    <a class="nav-link text-primary" href="{{path('a_propos')}}"><i class="fas fa-address-book"></i> A Propos</a>
                </li>
            </ul>

            <a class="navbar-brand  justify-content-between" href="{{path('accueil')}}">
            <img src="{{asset('images/ananse_logo_trans.png')}}" title="accueil" alt="ananse logo">
            </a>


            <ul class="navbar-nav ml-auto">

                <li class="nav-item dropdown">

                <a class="nav-link  text-primary" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                          {% if app.user %}<img src="{{ asset('images/'~app.user.avatar)}}" alt="avatar" class="img-fluid rounded-circle" style="width: 34px; height:34px;"> {{app.user.name}}{% else %}<i class="fas fa-users"></i> Espace utilisateur{% endif %} <i class="fas fa-caret-down"></i>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                       
                        {% if app.user %}
                        <a class="dropdown-item" href="{{path('mon_compte', {id: app.user.id})}}">Mon compte <i class="fas fa-user"></i></a>
                        <a class="dropdown-item" href="{{path('mes_candidatures', {id: app.user.id})}}">Mes candidatures</a>
                        <a class="dropdown-item" href="{{path('mes_annonces', {id: app.user.id})}}">Mes annonces</a>
                        <a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion <i class="fas fa-sign-out-alt"></i></a>
                        {% else %} 
                        <a class="dropdown-item" href="{{path('app_login')}}">Connexion <i class="fas fa-sign-in-alt"></i></a>
                            <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{path('app_register')}}">S'inscrire <i class="fas fa-user-plus"></i></a>
                        {% endif %}
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>