{% extends 'base.html.twig' %}

{% block title %}Hello PublisherController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1 class="text-center text-warning">Hello {{app.user.name}}</h1>
    <h2 class="text-center" >Les dernières nouvelles</h2>
    
   
</div>

{% for annonce in app.user.annonces %}
    {% for postulant in annonce.userPostulant %}
      
    {% endfor %}
{% endfor %}


<!-- On affiche la liste des postulants -->
{# <h3>{{name}}à postulé sur l'annonce : {{ annonce.titre }}</h3> 

 	{% if not annonce.UserPostulant.isEmpty %}
	{% for user in annonce.UserPostulant %}
		
	<div>
        <ul>
	
		    <div class="row">
				<li>
					{{user.name}}
					{{user.lastname}}
				</li>
			<a class="ml-5 pt-1 badge badge-pill badge-primary" href="{{path('mon_compte', {id: user.id})}} ">Voir son profil</a>
			</div>
		</ul>
	        </div>
	{% endfor %}
	{% else %}
	    <p> Aucun artiste n'a postulé à cette annonce pour le moment</p>
	{% endif %}  "#}
	</div>
{% endblock %}
